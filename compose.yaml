services:
  claimguard:
    build: .
    ports:
      - "8000:8000"
    environment:
      PORT: "8000"
      WEB_CONCURRENCY: "1"
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 2G
